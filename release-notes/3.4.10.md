---
date: 2026-07-19
codename: Aurora
---

Faster AI: the assistant now reuses its cached prompt across turns and agent steps, loads study tools and tutoring instructions only inside Deep Work, skips tools for disconnected integrations, and streams replies with less UI overhead. Conversation titles are generated on the fast model, and relevant-note lookups run in parallel before each reply.

Spark intro: the setup step now scrolls on small windows, and the window can be dragged while the intro is open. The app starts maximized.

Settings: reorganized into seven focused tabs — Account & plan, Connections & keys, AI behavior, Phone & notifications, Appearance, Data & storage, and About & updates — with a new Uninstall option that can delete all local data first.
