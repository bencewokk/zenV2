{
  "name": "zenv2-sync-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Serverless sync API for Zen V2 — Google ID-token auth + MongoDB Atlas (last-write-wins).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "vercel dev"
  },
  "dependencies": {
    "google-auth-library": "^10.9.0",
    "mongodb": "^6.12.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/web-push": "^3.6.4",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.6"
  }
}
